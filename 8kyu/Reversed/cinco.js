// Complete the solution so that it reverses the string passed into it.
// split("") - This method splits the string into an array of individual characters.
// reverse() - This method reverses the order of the characters in the array.
// join("") - This method joins the reversed characters back into a single string.
function solution(str){
  return str.split("").reverse().join("")
}